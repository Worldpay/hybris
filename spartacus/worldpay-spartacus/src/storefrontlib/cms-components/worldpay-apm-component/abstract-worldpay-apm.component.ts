export abstract class AbstractWorldpayApmComponent {
  code: string;

  constructor(
    // protected worldpayApmService: WorldpayApmService
  ) {
  }

  selectApm(): void {
    // this.worldpayApmService.selectAPM(this);
  }
}
