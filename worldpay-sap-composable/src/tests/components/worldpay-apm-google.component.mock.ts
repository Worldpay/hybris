import { Component, Input } from '@angular/core';
import { ApmData } from '../../core';

@Component({
  selector: 'y-worldpay-apm-googlepay',
  template: ''
})
export class MockWorldpayApmGooglepayComponent {
  @Input() apm: ApmData;
}